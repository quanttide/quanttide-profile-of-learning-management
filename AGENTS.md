# 学习管理档案设计

量潮学习云的学习管理档案（Learning Profile）：记录学员的验收标准通过状态与学习状态。

## 核心模型

学习档案两层：

```
Member (成员)
├── Criterion (验收标准) + 状态      # 验收层：谁通过了什么标准
└── Learning State (学习状态)        # 状态层：当前能力信号与成长轨迹
```

- **验收标准**由课程档案定义（单一事实源）
- **通过状态**由学习档案记录
- **学习状态**从教学过程（日志/交流/任务交付）提炼，是档案的快照层——随每次重要教学节点滚动更新

## 验收标准来源

课程开发档案（quanttide-profile-of-course-development）的 `index.json`：

```json
{
  "acceptance": {
    "criteria": ["验收标准1", "验收标准2"],
    "method": "self-check | real-world"
  }
}
```

## 学习档案格式

每位成员一个文件，用昵称命名：

```markdown
# {昵称} 学习档案

## 验收标准（课程定稿后启用）

- [x] 验收标准1
- [ ] 验收标准2

## 学习状态（{日期}）

### 当前状态：{阶段名}

{一段话概括}

### 能力信号

- {已观测到的能力证据}

### 成长特征

- {学习风格与动机特征}

### 待验证

- [ ] {待验证项（可勾选）}

### 教学动作

- {导师侧下一步}
```

## 学习状态提炼规则

- **快照非流水**：学习状态是提炼后的当前快照，随重要教学节点滚动更新（覆盖上一版），过程原始记录归 journal
- **只写已观测事实**：能力信号必须有教学交流/任务交付中的实际证据支撑；推断性判断标注"导师观察"
- **待验证用 checkbox**：可勾选、可追溯，完成后勾选并在下一轮状态更新中收敛
- **学员昵称即可**：档案内不引用日志路径，不写敏感来源

## 目录结构

```
data/profile/
└── {org}/
    ├── README.md
    ├── {nickname}.md
    └── ...
```
